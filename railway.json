{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pnpm install && pnpm turbo build --filter=@my-app/api"
  },
  "deploy": {
    "startCommand": "node apps/api/dist/index.js"
  }
}
